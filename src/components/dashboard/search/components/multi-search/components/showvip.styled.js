import styled from "styled-components";

const ShowVipWrapper = styled.section`
  margin: 30px 0px 0px 0px;

  .heading {
    margin: 20px 0px;
    text-align: center;
    color: var(--color-orange);
  }

  .notFound {
    border-top: 2px solid tomato;
    h2 {
      text-align: center;
      font-size: 1.6rem;
      color: tomato;
    }
  }
`;

export default ShowVipWrapper;
